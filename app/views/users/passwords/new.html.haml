.User-password
  = render 'shared/header'
  .User-password__title
    %i.fa.fa-key &nbsp; パスワードを忘れてしまった方へ
    .User-password__title__message
      %h1 登録しているメールアドレスを入力してください
  .User-password__main
  = form_with model: @user, as: resource_name, url: password_path(resource_name), local: true, method: :post, class: "User-session__main" do |f|
    %br
    %label.label email
    %br
    = f.email_field(:email, autofocus: true, autocomplete: "email", class: "text-field", placeholder: "PC・携帯どちらでも可")
    .User-password__main__submit
      = f.submit "送信", class: "submit-button"
-# = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
-#   = render "devise/shared/error_messages", resource: resource
-#   .field
-#     = f.label :email
-#     %br/
    -# = f.email_field :email, autofocus: true, autocomplete: "email"
-#   .actions
-#     = f.submit "Send me reset password instructions"
-# = render "users/shared/links"
